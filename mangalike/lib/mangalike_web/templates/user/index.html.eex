<!DOCTYPE html>
<html lang="en">

  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title>Users</title>
  </head>

  <body>
    <section class="jumbotron text-center">
      <div class="container">
        <h1 class="jumbotron-heading">Discover Users!</h1>
      </div>
    </section>


     <center> <div class="col-lg-8">


       <%= for user <- @users do %>
          <div class="p-3 mb-2 text-black" style="border-radius: 25px; border-color: #0c5460;border-width: 1px; border-style: solid;">
              <h2>Username: <%= user.username %></h2>
              <p>Name: <%= user.name %></p>
              <p>Email: <%= user.email %> </p>

              <p class="text-right">
                <span><%= link "Show", to: user_path(@conn, :show, user), class: "btn btn-primary btn-xs" %></span>
                <%= if (@current_user.is_admin?) || (@current_user.id == user.id) do %>
                <span><%= link "Edit", to: user_path(@conn, :edit, user), class: "btn btn-default btn-xs" %></span>
                <span><%= link "Delete", to: user_path(@conn, :delete, user), method: :delete, data: [confirm: "Are you sure?"], class: "btn btn-danger btn-xs" %></span>
                <% end %>
              </p>
            </div>
        <% end %>

        </div>
      </center>

  </body>
</html>